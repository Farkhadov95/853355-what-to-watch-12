export enum AppRoute {
  Login = '/login',
  Player = '/player',
  Root = '/',
  Film = '/film',
  MyList = '/myList',
  Review = '/review'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN'
}

export const DEFAULT_GENRE = 'All genres';

